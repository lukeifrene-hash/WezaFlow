[project]
name = "localflow"
version = "0.1.0"
description = "Local-first desktop dictation pipeline"
requires-python = ">=3.11"

[tool.localflow]
primary-platform = "windows"
experimental-platforms = ["macos"]
